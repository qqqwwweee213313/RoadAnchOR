# Checkpoints

This directory is ignored by git. See the [training guide](../docs/TRAINING.md#pre-trained-weights).

```
ckpts/
├── resnet50-19c8e357.pth   # torchvision ImageNet ResNet-50
└── vad_b2d_base.pth        # perception pre-training released with the upstream baseline
```
